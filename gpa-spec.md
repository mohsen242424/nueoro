# مواصفات حاسبة المعدل — الجامعة الهاشمية (نظام النقاط من 4.00)

نفّذ حاسبة المعدل في الموقع حسب هذه المواصفات بالضبط. المصدر: تعليمات منح درجة البكالوريوس في الجامعة الهاشمية، تعليمات رقم (8) لسنة 2016 وتعديلاتها.

---

## 1. جدول الرموز والنقاط (ثابت لا يتغير)

```js
const POINTS = {
  "A+": 4.00, "A": 3.75, "A-": 3.50,
  "B+": 3.25, "B": 3.00, "B-": 2.75,
  "C+": 2.50, "C": 2.25, "C-": 2.00,
  "D+": 1.75, "D": 1.50, "F":  0.00
};

const ARABIC = {
  "A+":"أ+","A":"أ","A-":"أ-","B+":"ب+","B":"ب","B-":"ب-",
  "C+":"ج+","C":"ج","C-":"ج-","D+":"د+","D":"د","F":"هـ"
};
```

- الحد الأدنى للنجاح في المادة: **(د) = 1.50**
- علامة الرسوب: **(هـ) = 0.00**
- الإدخال يكون **بالرمز من قائمة منسدلة**، وليس بالنسبة المئوية. لا تبنِ أي تحويل من النسبة إلى الرمز — لا يوجد جدول رسمي لذلك.

## 2. تقديرات المعدل

| النقاط | التقدير |
|---|---|
| 3.50 – 4.00 | ممتاز |
| 3.00 – 3.49 | جيد جداً |
| 2.50 – 2.99 | جيد |
| 2.00 – 2.49 | مقبول |
| دون 2.00 | ضعيف |

---

## 3. المعادلات

**نقاط المادة:** `نقاط الرمز × الساعات المعتمدة`

**المعدل الفصلي:** `مجموع(نقاط الرمز × الساعات) ÷ مجموع(الساعات)`

**التراكمي الجديد:**
```
النقاط = (المعدل التراكمي السابق × الساعات المقطوعة) + مجموع(نقاط الفصل × ساعاته)
الساعات = الساعات المقطوعة + ساعات الفصل
التراكمي = النقاط ÷ الساعات
```

**المعدل الفصلي المطلوب للوصول لهدف:**
```
المطلوب = [الهدف × (الساعات السابقة + ساعات الفصل) − التراكمي السابق × الساعات السابقة] ÷ ساعات الفصل
```
إذا كانت النتيجة > 4.00 → أظهر أن الهدف غير قابل للتحقيق. إذا ≤ 0 → الهدف محقق أصلاً.

**التدوير:** كل المعدلات تُعرض بمنزلتين عشريتين.

---

## 4. المواد المعادة (أهم جزء — انتبه له)

القاعدة الرسمية: عند إعادة المادة تُحتسب **العلامة الأعلى** بين المحاولتين في التراكمي، وتبقى الأخرى في السجل دون احتساب، وساعات المادة تدخل في ساعات التخرج **مرة واحدة فقط**.

خطأ شائع يجب تجنبه: «التراكمي السابق» و«الساعات المقطوعة» يحتويان أصلاً على العلامة القديمة، فيجب **طرحها أولاً** ثم إضافة المحتسبة، وإلا تُحتسب الساعة مرتين ويخرج المعدل غلط.

```js
// rows: [{ hours, grade, repeat: bool, oldGrade, excluded: bool }]
function newCumulative(prevGPA, prevHours, rows) {
  let qp = prevGPA * prevHours;
  let h  = prevHours;

  for (const r of rows) {
    if (r.excluded || !r.grade) continue;

    if (r.repeat && r.oldGrade) {           // إزالة المحاولة القديمة
      qp -= POINTS[r.oldGrade] * r.hours;
      h  -= r.hours;
    }
    const eff = (r.repeat && r.oldGrade)
      ? Math.max(POINTS[r.grade], POINTS[r.oldGrade])   // العلامة الأعلى
      : POINTS[r.grade];

    qp += eff * r.hours;
    h  += r.hours;
  }
  return h > 0 ? +(qp / h).toFixed(2) : 0;
}
```

**ملاحظة:** المعدل **الفصلي** يستخدم العلامة الجديدة فعلياً؛ قاعدة «العلامة الأعلى» تنطبق على **التراكمي** فقط.

**تحققات في الواجهة:**
- الإعادة لرفع المعدل مسموحة فقط إذا كانت العلامة السابقة **(ج+) = 2.50 أو أقل**، ولمرة واحدة.
- إذا اختار الطالب «معادة» بعلامة سابقة أعلى من (ج+) → أظهر تنبيهاً أن الإعادة غير مسموحة.

---

## 5. مواد لا تدخل في المعدل (خيار checkbox لكل مادة)

- المواد المعادَلة من جامعة أخرى أو من التجسير
- المواد المدروسة في جامعة أخرى بموافقة مسبقة
- المواد الاستدراكية
- الانسحاب (W) وغير المكتمل (I)

هذه لا تدخل في المعدل الفصلي ولا التراكمي، ولا تُحتسب ساعاتها في المعدل.

---

## 6. المخرجات المطلوبة عند الضغط على زر «احسب»

أظهر للطالب بطاقة نتيجة تحتوي على:

1. **المعدل التراكمي بعد هذا الفصل** (رقم كبير) + **التقدير** بجانبه
2. **المعدل الفصلي** + تقديره
3. **ساعات الفصل** و **مجموع الساعات المحتسبة**
4. **الحالة الأكاديمية:**
   - تراكمي < 1.50 → «تراكمي دون 1.50 — خطر الفصل» (أحمر)
   - تراكمي < 2.00 → «إنذار أكاديمي» (أحمر)
   - تراكمي ≥ 2.00 → «مؤهل للتخرج بهذا التراكمي» (أخضر)
   - معدل فصلي ≥ 3.50 مع ساعات فصلية ≥ 15 → «لائحة شرف الكلية»
   - وجود مواد بعلامة (هـ) → «عدد المواد الراسبة التي تحتاج إعادة»
5. **جدول تفصيلي** بكل مادة: الاسم، الساعات، الرمز، النقاط، النقاط × الساعات، وملاحظة «معادة — احتُسبت العلامة الأعلى» عند اللزوم
6. **كم لازمك:** حقل للتراكمي المستهدف وساعات الفصل القادم، ويظهر المعدل الفصلي المطلوب + أقرب رمز يقابله

---

## 7. حالات اختبار — يجب أن تعطي هذه النتائج بالضبط

| # | المدخلات | النتيجة المتوقعة |
|---|---|---|
| 1 | بدون سجل سابق: A (3سا)، B+ (3سا)، B (3سا) | فصلي = **3.33**، تراكمي = **3.33**، التقدير: جيد جداً |
| 2 | سابق 3.00 و60 ساعة + نفس مواد الحالة 1 | تراكمي = **3.04** (210 ÷ 69) |
| 3 | سابق 2.50 و30 ساعة، مادة 3سا كانت (هـ) وأعادها وجاب B | تراكمي = **2.80**، الساعات = 30 (لا تزيد) |
| 4 | سابق 2.50 و30 ساعة، مادة 3سا كانت C+ وأعادها وجاب C | تراكمي = **2.50** (تُحتسب C+ لأنها الأعلى) |
| 5 | سابق 2.80 و60 ساعة، الهدف 3.00 بـ 15 ساعة | المطلوب فصلياً = **3.80** |
| 6 | مادة مؤشر عليها «لا تدخل في المعدل» | لا تؤثر على أي معدل ولا على الساعات |

---

## 8. متطلبات الواجهة

- دعم RTL بالكامل، وتصميم يعمل على الهاتف (معظم الاستخدام من الجوال)
- إضافة وحذف صفوف المواد ديناميكياً
- الحساب يتم عند الضغط على زر «احسب»، مع عرض النتائج والتفاصيل أسفل النموذج
- التحقق من المدخلات: المعدل السابق بين 0 و4.00، الساعات أرقام موجبة، رسالة واضحة إذا كانت الحقول ناقصة
- جدول الرموز والنقاط ظاهر في الصفحة كمرجع للطالب (قابل للطي)
- ملاحظة أسفل الحاسبة: «النتيجة تقديرية ولا تُغني عن كشف العلامات الرسمي»

---

## 9. ما يجب عدم فعله

- لا تستخدم سلّم نقاط مختلف (مثل A=4.00) — الصحيح **A+=4.00 و A=3.75** بفروقات 0.25
- لا تحوّل النسبة المئوية إلى رمز
- لا تعُدّ ساعات المادة المعادة مرتين
- لا تعرض تحويلاً «رسمياً» من 4 إلى 100 — إن عُرض فليكن مكتوباً بوضوح أنه تقريبي
